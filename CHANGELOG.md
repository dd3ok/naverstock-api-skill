# 변경 이력

사용자에게 영향을 주는 변경과 검증 범위를 기록합니다. 날짜는 `main`에 반영한 날이며, 버전 태그를 발행했다는 뜻은 아닙니다.

## 2026-09-07 — main 반영, 새 버전 미출시

### 리서치 소량 검증과 요청 처리 개선 — PR #14

[PR #14](https://github.com/dd3ok/naverstock-api-skill/pull/14) · 머지 커밋 `bf94b07`

- **Python 지원 변경:** Python 3.14의 최신 패치 버전만 지원·검증합니다. Python 3.10~3.13 CI 매트릭스와 호환성 유지 정책을 종료했습니다.
- **리서치 검증 도구 추가:** `research_check.py`는 기본 실행에서 요청 계획만 출력합니다. `--live`를 지정하면 한 카테고리의 첫·다음 페이지를 최대 2회 조회하고, 첫 응답 처리 후 1초 이상 기다립니다. 응답 구조·정상 빈 목록·ID 중복·실패·미실행을 구분합니다.
- **접근 제한 후 중단:** `research.py home`은 403·429·3xx·비정상 JSON에서 후속 조회를 멈춥니다. 완료한 결과와 실패 섹션을 보존하고 이후 섹션은 `not_run`으로 표시합니다. 일반 404·500의 부분 실패 처리는 유지합니다.
- **요청 전 입력 검증:** 선택한 업종 페이지·페이지 크기와 리서치 홈의 활성 섹션 조건을 첫 요청 전에 검증합니다. 사용하지 않는 업종·홈 섹션 옵션은 검증하지 않습니다.
- **잘못된 페이지·날짜 거부:** 리서치 v2 목록의 0·음수 페이지를 첫 페이지로 보정하던 동작을 제거했습니다. `YYYY-MM-DD`·`YYYYMMDD`의 실제 달력 날짜와 시작일·종료일 순서를 검증합니다. 유효 입력의 페이지 상한과 v1의 0-based `index`는 유지합니다.
- **오류 종류 구분:** 검증 요약의 `errorKind`로 타임아웃·잘못된 JSON·응답 크기 초과 등을 구분합니다. 원격 오류 원문을 요약에 포함하지 않으며 기존 `NaverStockAPIError.as_dict()` 형식은 유지합니다.
- **유지보수 검사:** 문서의 로컬 링크·섹션 앵커 검사와 설치본의 기본 계획 실행 검사를 추가하고 Ruff 버전을 고정했습니다.

진행 상태 파일, 자동 재시도·재개, 새 런타임 의존성은 추가하지 않았습니다. 결과 파일은 `--output`을 명시한 경우에만 저장합니다. [사용법과 결과 해석](references/script-cookbook.md#리서치-소량-검증)을 참고하세요.

**검증:** [머지 후 CI](https://github.com/dd3ok/naverstock-api-skill/actions/runs/34101410655)에서 Python 3.14.7의 282개 테스트와 전체 단계가 통과했습니다. COMPANY v2의 `index=0/1&size=2` 실응답은 각각 2건이며 ID 비중복을 확인했습니다. 이 표본으로 모든 카테고리·필터·끝 페이지나 금융 수치 정확성을 검증한 것은 아닙니다.

### 네이버 API 관찰 기록과 안전장치 갱신 — PR #13

[PR #13](https://github.com/dd3ok/naverstock-api-skill/pull/13) · 머지 커밋 `ed15397`

- 현재 네이버증권 화면·하위 탭·연결 페이지·정적 코드와 실응답을 대조해 API 경로, 파라미터, 페이징, 관찰 상태를 갱신했습니다.
- AI 시장 브리핑의 명시적 v2 선택과 해외 ETF 테마 조건을 보강했습니다. 기존 v1 조회를 자동으로 다른 응답 계약으로 바꾸지 않습니다.
- 국내 ASCII 영숫자 상품 코드와 해외 상품 코드의 필요한 대소문자를 보존하도록 식별자 처리를 보강했습니다. WiseReport의 숫자 6자리 제한은 유지합니다.
- 리다이렉트 후속 요청 차단, 응답 크기 제한, 조기 연결 종료와 레거시 필수 표·헤더 누락 검사를 보강했습니다.
- 종목 필터가 적용되지 않는 일반 토론 요청은 호출 전에 거부하고 전용 종목 조회를 안내합니다. 확인된 404·500에는 조건에 맞는 안내를 추가하며, 자동 fallback이나 임의 정렬·테마 변경은 하지 않습니다.
- 리서치·해외뉴스·미국 섹터의 표본 페이징과 펀드 클래스의 비어 있지 않은 응답을 확인했습니다. 확인하지 못한 조건과 서버 실패는 [알려진 제한과 검증 범위](references/known-limitations.md)에 남겼습니다.

**검증:** [당시 머지 후 CI](https://github.com/dd3ok/naverstock-api-skill/actions/runs/34096382915)에서 당시 지원하던 Python 3.10~3.13의 각 256개 테스트가 통과했습니다. 현재 Python 지원 정책은 위 PR #14의 변경을 따릅니다. HTTP 성공, 상품·필터 일치, 페이징 완료, 수치 정확성은 별도 판정입니다.
